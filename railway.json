{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @sweep/api db:generate && pnpm --filter @sweep/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @sweep/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
